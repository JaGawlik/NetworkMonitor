<UserControl x:Class="NetworkMonitor.Windows.Views.AdminSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NetworkMonitor.Windows.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
        <!-- Lista najczęstszych alertów -->
        <TextBlock Text="Najczęstsze alerty:" FontWeight="Bold" Margin="0,10,0,5" />
        <ListView x:Name="FrequentAlertsList" SelectionMode="Single" Margin="0,0,0,10">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="SID" DisplayMemberBinding="{Binding Sid}" Width="100" />
                    <GridViewColumn Header="Wiadomość" DisplayMemberBinding="{Binding Message}" Width="300" />
                    <GridViewColumn Header="Ilość" DisplayMemberBinding="{Binding Count}" Width="100" />
                </GridView>
            </ListView.View>
        </ListView>

        <!-- Dodawanie reguły z wybranej pozycji -->
        <Button Content="Dodaj regułę" Click="AddSelectedAlertRule_Click" HorizontalAlignment="Left" />

        <!-- Zapis zmian -->
        <Button Content="Zapisz zmiany" Click="SaveChangesButton_Click" HorizontalAlignment="Left" Margin="0,10,0,0" />
        </StackPanel>
    </Grid>
</UserControl>
