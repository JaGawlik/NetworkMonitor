<UserControl x:Class="NetworkMonitor.Windows.Views.AdminSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:windows="clr-namespace:NetworkMonitor.Windows" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Lista najczęstszych alertów -->
        <ListBox x:Name="FrequentAlertsList" Grid.Row="0" 
         ItemsSource="{Binding FrequentAlerts}" 
         SelectedItem="{Binding SelectedFrequentAlert}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="SID: " FontWeight="Bold"/>
                        <TextBlock Text="{Binding Sid}" Margin="5,0"/>
                        <TextBlock Text=" | Wiadomość: " FontWeight="Bold"/>
                        <TextBlock Text="{Binding Message}" Margin="5,0"/>
                        <TextBlock Text=" | Liczba wystąpień: " FontWeight="Bold"/>
                        <TextBlock Text="{Binding Count}" Margin="5,0"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Przyciski akcji -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="10">
            <Button Content="Dodaj regułę" Click="AddSelectedAlertRule_Click" Margin="0,0,10,0"/>
            <Button Content="Zapisz zmiany" Click="SaveChangesButton_Click"/>
        </StackPanel>
    </Grid>
</UserControl>
