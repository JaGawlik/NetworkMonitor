<UserControl x:Class="NetworkMonitor.Windows.Views.ConfigurationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NetworkMonitor.Windows.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Margin="20">
            <TextBlock Text="Ścieżka do pliku logów Snort:" FontWeight="Bold" Margin="5,0,5,0" />
            <Grid Margin="0,5,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox Text="{Binding LogFilePath}" />
                <Button Grid.Column="1" Content="..." Width="30" Margin="5,0,0,0" 
                        Command="{Binding BrowseLogFileCommand}" />
            </Grid>

            <TextBlock Text="Adres API:" FontWeight="Bold" Margin="5,0,5,0"/>
            <TextBox Text="{Binding ApiAddress}" Height="25" Margin="0,5,0,15"  />

            <TextBlock Text="Folder instalacyjny Snort:" Margin="5,0,5,0" FontWeight="Bold"/>
            <Grid Margin="0,5,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox Text="{Binding SnortInstallPath}" />
                <Button Grid.Column="1" Content="..." Width="30" Margin="5,0,0,0" 
                        Command="{Binding BrowseSnortFolderCommand}" />
            </Grid>

            <Button Content="Zapisz ustawienia" Width="150" HorizontalAlignment="Left" 
                Command="{Binding SaveCommand}" />
        </StackPanel>
    </Grid>
</UserControl>
