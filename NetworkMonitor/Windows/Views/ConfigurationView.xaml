<UserControl x:Class="NetworkMonitor.Windows.Views.ConfigurationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NetworkMonitor.Windows.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Margin="20">
            <TextBlock Text="Ścieżka do pliku logów Snort:" FontWeight="Bold" />
            <StackPanel Orientation="Horizontal">
                <TextBox Text="{Binding LogFilePath}" Width="450" Height="25" Margin="0,5,5,15" />
                
            </StackPanel>

            <TextBlock Text="Adres API:" FontWeight="Bold" />
            <TextBox Text="{Binding ApiAddress}" Height="25" Margin="0,5,0,15"  />

            <TextBlock Text="Folder instalacyjny Snort:" Margin="0,0,0,5" FontWeight="Bold"/>
            <StackPanel Orientation="Horizontal">
                <TextBox Text="{Binding SnortInstallPath}" Width="450" Height="25" Margin="0,0,5,10" />
               
            </StackPanel>

            <Button Content="Zapisz ustawienia" Width="150" HorizontalAlignment="Left" 
                Command="{Binding SaveCommand}" />
        </StackPanel>
    </Grid>
</UserControl>
