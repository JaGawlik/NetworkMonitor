<UserControl x:Class="NetworkMonitor.Windows.Views.AlertsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:NetworkMonitor.Windows"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid ItemsSource="{Binding AlertGroupViewModels}" AutoGenerateColumns="False" IsReadOnly="True" Margin="10">
            <!-- Kolumny główne -->
            <DataGrid.Columns>
                <DataGridTextColumn Header="Destination IP" Binding="{Binding DestinationIp}" Width="*" />
                <DataGridTextColumn Header="Alert Count" Binding="{Binding Alerts.Count}" Width="*" />
            </DataGrid.Columns>

            <!-- Szczegóły rozwijanego wiersza -->
            <DataGrid.RowDetailsTemplate>
                <DataTemplate>
                    <Border BorderBrush="Gray" BorderThickness="1" Padding="5">
                        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Height="150">
                            <DataGrid ItemsSource="{Binding Alerts}" AutoGenerateColumns="False" IsReadOnly="True">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Timestamp" Binding="{Binding Timestamp, StringFormat=yyyy-MM-dd HH:mm:ss}" Width="*" />
                                    <DataGridTextColumn Header="Alert Message" Binding="{Binding AlertMessage}" Width="*" />
                                    <DataGridTextColumn Header="Protocol" Binding="{Binding Protocol}" Width="*" />
                                    <DataGridTextColumn Header="Source IP" Binding="{Binding SourceIp}" Width="*" />
                                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="*" />
                                </DataGrid.Columns>
                            </DataGrid>
                        </ScrollViewer>
                    </Border>
                </DataTemplate>
            </DataGrid.RowDetailsTemplate>
        </DataGrid>
    </Grid>
</UserControl>
