<UserControl x:Class="NetworkMonitor.Windows.Views.AlertsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NetworkMonitor.Windows.Views" 
             xmlns:vm="clr-namespace:NetworkMonitor.Windows" 
             xmlns:networkmonitor="clr-namespace:NetworkMonitor" 
             d:DataContext="{d:DesignInstance Type=networkmonitor:MainWindowViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <TreeView ItemsSource="{Binding AlertGroupViewModels}" Margin="10">
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate DataType="{x:Type vm:AlertGroupViewModel}" ItemsSource="{Binding Alerts}">
                    <!-- Wyświetlanie grupy -->
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding DestinationIp}" FontWeight="Bold" Margin="0,0,10,0" />
                        <TextBlock Text="(" />
                        <TextBlock Text="{Binding Alerts.Count}" />
                        <TextBlock Text=" alerts)" />
                    </StackPanel>

                    <!-- Wyświetlanie szczegółów alertu -->
                    <HierarchicalDataTemplate.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Timestamp, StringFormat=yyyy-MM-dd HH:mm:ss}" Margin="0,0,10,0" />
                                <TextBlock Text="{Binding AlertMessage}" Margin="0,0,10,0" />
                                <TextBlock Text="{Binding Protocol}" Margin="0,0,10,0" />
                                <TextBlock Text="{Binding SourceIp}" Margin="0,0,10,0" />
                                <TextBlock Text="{Binding Status}" />
                            </StackPanel>
                        </DataTemplate>
                    </HierarchicalDataTemplate.ItemTemplate>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>
    </Grid>
</UserControl>
